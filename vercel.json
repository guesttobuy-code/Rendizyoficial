{
  "buildCommand": "cd RendizyPrincipal && npm install && npm run build",
  "outputDirectory": "RendizyPrincipal/dist",
  "framework": "vite",
  "installCommand": "cd RendizyPrincipal && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
